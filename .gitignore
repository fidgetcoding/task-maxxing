# Secrets and credentials — never commit
.env
.env.*
!.env.example
*.pem
*.key
*.pfx
*.cert
*.crt
credentials.json
secrets.json

# Local sync state (generated at runtime, not part of the template)
.sync-state.json

# Installer tmp artifacts — these contain substituted secrets in transit
# and should never land in the repo even if someone runs the installer
# with a non-standard TMPDIR that points at this tree.
*.rendered.json
*.clean.json

# Node / JavaScript
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS cruft
.DS_Store
Thumbs.db
Desktop.ini

# Logs
*.log
logs/

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~
.project
.classpath

# Build output
dist/
build/
coverage/
.nyc_output/
